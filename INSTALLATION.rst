============
Installation
============

At the command line::

    $ pip install securitygroupmanagerlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv securitygroupmanagerlib
    $ pip install securitygroupmanagerlib

Or, if you are using pipenv::

    $ pipenv install securitygroupmanagerlib

